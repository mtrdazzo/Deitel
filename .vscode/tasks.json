{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "run makefile",
            "command": "make",
            "options": {
                "cwd": "${fileDirname}/.."
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}